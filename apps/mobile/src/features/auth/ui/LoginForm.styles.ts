import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  form: {
    gap: 4,
  },
  field: {
    marginTop: 12,
  },
  formError: {
    marginBottom: 4,
  },
  submitButton: {
    marginTop: 20,
  },
});
